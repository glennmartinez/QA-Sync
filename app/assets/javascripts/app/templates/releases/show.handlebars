<td>{{release.id}}</td>
<td class="data">
{{#if isEditing}}
    {{view App.EditReleaseView}}
  {{else}}
    {{release.fullData}}
  {{/if}}
    <div class="commands">
    {{#unless isEditing}}
      <a href="#" {{action "showEdit"}}>Edit</a>
      <a href="#" {{action "destroyRecord"}}>Remove</a>
    {{/unless}}
  </div>

</td>